- name: Download Helm
  ansible.builtin.get_url:
    url: https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3
    dest: /tmp/get_helm.sh
    mode: 700

# - name: Install Helm
#   ansible.builtin.shell: /tmp/get_helm.sh

# - name: Install K3s with Flannel CNI
#   ansible.builtin.shell: curl -sfL https://get.k3s.io | sh -

# - name: Copy Cilium CNI CLI script
#   ansible.builtin.copy:
#     src: ../../../files/install-cilium.sh
#     dest: /tmp/install-cilium.sh
#     mode: 700

# - name: Execute Cilium CNI CLI script
#   ansible.builtin.shell: /tmp/install-cilium.sh
#   retries: 3
#   delay: 10

# - name: Install Cilium CNI
#   ansible.builtin.shell: |
#     export KUBECONFIG=/etc/rancher/k3s/k3s.yaml
#     cilium install --version 1.18.1
